/**
 * @format
 */

import {AppRegistry} from 'react-native';
import NewStack from './Src/app/NewStack';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => NewStack);
